# คู่มือกองบรรณาธิการ — การจัดหน้าบทความบน WordPress

เว็บไซต์ prachatham.com เป็นระบบ **Headless CMS**: WordPress ใช้สำหรับ*เขียนและจัดการเนื้อหาเท่านั้น* ส่วนหน้าตา สี ฟอนต์ และ layout ทั้งหมด ตัวเว็บไซต์เป็นผู้กำหนดเองโดยอัตโนมัติ

**หลักคิดสำคัญ: เขียนเนื้อหาให้ดี แล้วปล่อยให้เว็บจัดสวยเอง** สิ่งที่ตกแต่งเองใน WordPress (สีตัวอักษร ขนาดฟอนต์ ฯลฯ) จะ*ถูกลบทิ้ง*เพื่อรักษาความสม่ำเสมอของทั้งเว็บ

> ⏱ หลังกดเผยแพร่/อัปเดต เว็บจะแสดงผลใหม่ภายใน **ประมาณ 1 นาที** (มีระบบ cache) — รอสักครู่แล้วค่อยรีเฟรชดู

---

## 1. บล็อก (Block) ที่ใช้ได้ และผลลัพธ์บนเว็บ

ใช้บล็อกมาตรฐานของ WordPress (Gutenberg) เท่านั้น ตารางนี้คือสิ่งที่จะเห็นบนเว็บจริง:

| บล็อกใน WordPress | ผลลัพธ์บนเว็บไซต์ |
|---|---|
| **ย่อหน้า (Paragraph)** | ตัวอักษรขนาดอ่านสบาย ระยะห่างบรรทัดอัตโนมัติ / จัดกึ่งกลาง-ชิดขวาได้ |
| **หัวข้อ (Heading) H2** | ตัวหนาใหญ่ มีเส้นใต้สีเขียว + **ขึ้นในสารบัญอัตโนมัติ** |
| **หัวข้อ (Heading) H3** | ตัวหนารองลงมา ไม่มีเส้นใต้ + ขึ้นในสารบัญ (ย่อหน้าเข้าไปหนึ่งระดับ) |
| **รูปภาพ (Image)** | มุมโค้งอัตโนมัติ / จัดกึ่งกลาง-ซ้าย-ขวาได้ / **ย่อขนาดในเอดิเตอร์ได้ เว็บจะแสดงตามนั้น** / คำบรรยายใต้ภาพ (caption) แสดงตัวเล็กสีเทากึ่งกลาง |
| **แกลเลอรี (Gallery)** | เรียงเป็นตาราง 3 คอลัมน์ (เลือก 2 หรือ 4 คอลัมน์ได้) รูปถูก crop ให้เท่ากันสวยงาม / บนมือถือเรียงลงมา |
| **รายการ (List)** | จุดนำ (•) หรือเลขลำดับ พร้อมระยะย่อหน้าอัตโนมัติ |
| **คำพูดยกมา (Quote)** | มีเส้นสีเขียวเข้มด้านซ้าย ตัวเอียงสีเทา |
| **ตาราง (Table)** | ตารางมีเส้นขอบ มุมโค้ง / บนมือถือเลื่อนซ้าย-ขวาได้ ไม่ล้นจอ |
| **ตัวคั่น (Separator)** | เส้นสั้นกึ่งกลาง / สไตล์ "กว้าง" = เส้นเต็มความกว้าง / สไตล์ "จุด" = ··· |
| **ข้อความพรีฟอร์แมต (Preformatted)** | ⭐ กลายเป็น**กล่องไฮไลต์สีเขียวอ่อน มีไอคอนเอกสาร** — เหมาะสำหรับกล่อง "ดาวน์โหลดเอกสาร" "แหล่งข้อมูล" "หมายเหตุ" |
| **ปุ่ม (Buttons)** | ปุ่มทรงแคปซูลสีเขียวแบรนด์ ตัวหนังสือขาว — ใช้สำหรับลิงก์ดาวน์โหลด/ลิงก์สำคัญ |
| **ฝังวิดีโอ YouTube** | วิดีโอเต็มความกว้าง สัดส่วน 16:9 ปรับตามขนาดจออัตโนมัติ |
| **เชิงอรรถ (Footnotes)** | กลายเป็นกล่อง "อ้างอิง" แบบพับเก็บได้ท้ายบทความ ผู้อ่านกดเปิดเอง |

### ❌ บล็อกที่ห้ามใช้

- **บล็อกตระกูล Kadence ทุกตัว** (Spacer, Advanced Gallery, Advanced Button, Row Layout) — เว็บไม่รองรับ ใช้บล็อกมาตรฐานแทน
- **Custom HTML** ที่มี `<script>` หรือ `<style>` — จะถูกลบทิ้งด้วยระบบความปลอดภัย

---

## 2. หัวข้อกับสารบัญ (สำคัญมาก)

- **ชื่อบทความ = H1 อยู่แล้ว** ในเนื้อหาให้เริ่มที่ **H2** สำหรับหัวข้อหลัก และ **H3** สำหรับหัวข้อย่อย
- ทุก H2/H3 จะปรากฏใน**สารบัญ**อัตโนมัติ (แถบข้างบนจอใหญ่ / ปุ่มลอยบนมือถือ)
- เขียนหัวข้อให้สั้น กระชับ ได้ใจความ — เพราะมันคือเมนูนำทางของผู้อ่าน
- ใช้เครื่องหมายคำพูด "..." อัญประกาศ และอักขระพิเศษในหัวข้อได้ตามปกติ เว็บแสดงผลถูกต้อง

---

## 3. รูปภาพ

| สิ่งที่ทำ | ผลลัพธ์ |
|---|---|
| จัดกึ่งกลาง (Align center) | รูปอยู่กึ่งกลางคอลัมน์ ✓ |
| จัดชิดซ้าย/ขวา (Align left/right) | ข้อความไหลอ้อมรูป (บนมือถือรูปจะเต็มความกว้างแทน) |
| ลากย่อขนาดรูปในเอดิเตอร์ | เว็บแสดงขนาดตามที่ย่อ ✓ |
| ใส่ caption ใต้รูป | แสดงตัวเล็กสีเทากึ่งกลางใต้รูป |
| ใส่ **Alt text** | ไม่เห็นบนจอ แต่สำคัญมากต่อ SEO และผู้พิการทางสายตา — **ใส่ทุกรูป** |

- อัปโหลดรูปเข้าคลังสื่อ (Media Library) ของ WordPress เท่านั้น อย่า hotlink รูปจากเว็บอื่น
- รูปควรกว้างอย่างน้อย ~1,200px สำหรับรูปหลัก

---

## 4. แท็ก HTML พื้นฐาน (สำหรับคนที่แก้ในโหมด Code)

| แท็ก | ผลลัพธ์บนเว็บ |
|---|---|
| `<strong>` หรือ `<b>` | **ตัวหนา** (สีเข้มกว่าปกติ) |
| `<em>` หรือ `<i>` | *ตัวเอียง* (สีเทา) |
| `<mark>` | ไฮไลต์พื้นเหลือง |
| `<a href="...">` | ลิงก์สีเขียวเข้ม มีเส้นใต้เมื่อ hover (รับเฉพาะ http/https/mailto/tel) |
| `<code>` | กล่องเทาเล็กในบรรทัด สำหรับโค้ด/คำสั่ง |
| `<pre>` | กล่องโค้ดพื้นเข้มตัวขาว |
| `<hr>` | เส้นคั่นแนวนอน |
| `<sup>` / `<sub>` | ตัวยก / ตัวห้อย |
| `<blockquote>` | กล่องคำพูดเส้นเขียวซ้าย ตัวเอียง |
| `<figure>` + `<figcaption>` | รูป + คำบรรยายใต้รูป |
| `<details>` + `<summary>` | กล่องพับเก็บ กดเปิด-ปิดได้ |

### สิ่งที่ระบบลบทิ้งอัตโนมัติ (เพื่อความปลอดภัยและความสม่ำเสมอ)

- `<script>`, `<style>`, `<form>`, `<input>` และ attribute จำพวก `onclick`
- **สีตัวอักษร ขนาดฟอนต์ และ inline style ทุกชนิด** — ยกเว้นขนาดรูปภาพ
- `<iframe>` จากเว็บอื่นนอกเหนือจากรายการที่อนุญาต (ดูข้อ 5)

---

## 5. การฝังสื่อ (Embed) ที่อนุญาต

ฝังได้เฉพาะจากแหล่งเหล่านี้ — แหล่งอื่นจะถูกลบทิ้ง:

- **YouTube** (แนะนำ — แสดง 16:9 responsive)
- Vimeo
- Facebook
- Instagram
- Spotify (พอดแคสต์)
- Google Maps

วิธีฝัง: วาง URL ลงในย่อหน้าเปล่า WordPress จะสร้างบล็อก embed ให้เอง

---

## 6. ข้อมูลประกอบบทความ (ต้องใส่ทุกครั้ง)

| ช่อง | ใช้ทำอะไร |
|---|---|
| **รูปหน้าปก (Featured Image)** | แสดงบนการ์ดข่าวหน้าแรก, หน้ารวมข่าว และตอนแชร์ลง Facebook/X — แนะนำแนวนอน ~1200×630px |
| **เกริ่นนำ (Excerpt)** | ข้อความบนการ์ดข่าว + คำอธิบายใน Google (ควรยาวไม่เกิน ~160 ตัวอักษร ถ้าไม่ใส่ระบบจะตัดจากย่อหน้าแรก) |
| **หมวดหมู่ (Category)** | กำหนดว่าบทความไปโผล่ที่ไหน — ดูข้อ 7 |
| **ชื่อผู้เขียน (ช่อง authornamepost)** | แสดงเป็นชื่อผู้เขียนบนหน้าบทความและการ์ดข่าว |

---

## 7. หมวดหมู่พิเศษ

| หมวด | พฤติกรรม |
|---|---|
| **ข่าวเด่น** | บทความจะขึ้น**หน้าแรกของเว็บ** (ส่วน Featured News สูงสุด 6 ชิ้น) — ใส่เฉพาะข่าวที่ต้องการดันจริง ๆ |
| **publication** | *ไม่*แสดงในหน้ารวมข่าวสาร — สำหรับหน้าสิ่งพิมพ์/เอกสารเผยแพร่โดยเฉพาะ |
| หมวดอื่น ๆ | แสดงเป็นชิปหัวข้อ "เลือกอ่านตามหัวข้อ" บนหน้าแรก และมีหน้ารวมของตัวเองที่ `/category/ชื่อหมวด` |

---

## 8. เช็กลิสต์ก่อนกดเผยแพร่

- [ ] หัวข้อในเนื้อหาใช้ H2/H3 (ไม่ใช่ตัวหนาเฉย ๆ) → สารบัญจะได้ทำงาน
- [ ] ใส่รูปหน้าปก (Featured Image)
- [ ] ใส่เกริ่นนำ (Excerpt)
- [ ] รูปทุกรูปมี Alt text
- [ ] เลือกหมวดหมู่ถูกต้อง (ระวังหมวด "ข่าวเด่น")
- [ ] ไม่ได้ใช้บล็อก Kadence / ไม่ได้ตั้งสี-ขนาดฟอนต์เอง
- [ ] วิดีโอ/เสียง ฝังจากแหล่งที่อนุญาตเท่านั้น
- [ ] เผยแพร่แล้วรอ ~1 นาที ค่อยเปิดเว็บจริงตรวจอีกรอบ

---

*อัปเดตล่าสุด: กรกฎาคม 2569 — หากพบว่าจัดหน้าในเอดิเตอร์แล้วเว็บแสดงผลไม่ตรง แจ้งทีมเทคนิคพร้อมลิงก์บทความ*
